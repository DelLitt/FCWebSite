(function () {
    'use strict';

    angular
        .module('fc.admin')
        .controller('rootCtrl', rootCtrl);

    rootCtrl.$inject = ['$scope'];

    function rootCtrl($scope) {
    }
})();
